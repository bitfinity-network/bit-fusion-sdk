```
$ bridge-deployer -v --bridge-network localhost --evm-rpc $EVM_RPC --private-key $PRIVATE_KEY --identity $IDENTITY_PATH upgrade --wasm $BRC20_BRIDGE_WASM_PATH --canister-id $BRC20_BRIDGE_ID
Starting Bitfinity Deployer v[..]
Canister [..] upgraded successfully

```
