```
$ bridge-deployer --silent --evm-network localhost --evm $EVM_PRINCIPAL --private-key $PRIVATE_KEY --identity $IDENTITY_PATH deploy --wasm $WASM_PATH --wallet-canister $WALLET_CANISTER rune --owner $ADMIN_PRINCIPAL --min-confirmations 6 --indexer-urls "https://indexer1.com,https://indexer2.com,https://indexer3.com" --deposit-fee 1000000 --mempool-timeout 3600 --signing-key-id dfx --bitcoin-network regtest --indexer-consensus-threshold 3
Starting Bitfinity Deployer v[..]
Canister deployed with ID [..]
BFT bridge deployed with address [..]; wrapped_token_deployer: [..]
Canister Rune deployed with ID [..]

```
