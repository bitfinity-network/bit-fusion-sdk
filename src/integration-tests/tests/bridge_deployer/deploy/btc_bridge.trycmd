```
$ bridge-deployer -v --evm-network localhost --evm $EVM_PRINCIPAL --private-key $PRIVATE_KEY --identity $IDENTITY_PATH deploy --wasm $BTC_BRIDGE_WASM_PATH --wallet-canister $WALLET_CANISTER btc --owner $ADMIN_PRINCIPAL --signing-key-id dfx --enable-console --log-filter trace --ledger $CKBTC_LEDGER --minter $CKBTC_MINTER --network regtest --fee 1000
Starting Bitfinity Deployer v[..]
Canister deployed with ID [..]
BFT bridge deployed with address [..]; wrapped_token_deployer: [..]
Canister rune-bridge deployed with ID [..]

```
