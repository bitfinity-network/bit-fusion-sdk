```
$ bridge-deployer -v --bridge-network localhost --evm-canister $EVM_PRINCIPAL --private-key $PRIVATE_KEY --identity $IDENTITY_PATH upgrade --wasm $ICRC2_BRIDGE_WASM_PATH --canister-id $ICRC2_BRIDGE_ID
Starting Bitfinity Deployer v[..]
Canister [..] upgraded successfully

```
