pub mod canister;
mod core;
mod state;
mod memory;

pub fn idl() -> String {
    // use api::{CanisterType, ParentInitArgs};
    // use candid::Principal;
    // use canister_sdk::ic_canister::{generate_idl, Idl};

    // let parent_idl = generate_idl!();

    // candid::bindings::candid::compile(&parent_idl.env.env, &Some(parent_idl.actor))

    todo!()
}
